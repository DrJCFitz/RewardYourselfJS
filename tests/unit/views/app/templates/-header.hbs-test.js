import {
  moduleFor,
  test
} from 'ember-qunit';

moduleFor('view:app/templates/-header.hbs', 'AppTemplatesHeader.HbsView');

// Replace this with your real tests.
test('it exists', function() {
  var view = this.subject();
  ok(view);
});
