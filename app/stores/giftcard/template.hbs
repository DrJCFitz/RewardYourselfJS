<div class="form-group">
    <input id="searchStoresInput" class="form-control" type="search" placeholder="Search Stores..."/>
</div>
<div id="searchStoresList" class="list-group">
    {{#each store in model}}
        {{#link-to 'giftcard.offer' store.id classNames='list-group-item'}}<span>{{store.name}}</span>{{/link-to}}
    {{else}}
    No stores found
    {{/each}}
</div>